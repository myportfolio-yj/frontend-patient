import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appomsv',
  appName: 'appomsv',
  webDir: 'dist/apposmv',
  server: {
    androidScheme: 'https'
  }
};

export default config;
